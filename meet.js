const meetNav = document.createElement('button');
meetNav.className = 'nav-item'; meetNav.dataset.target = 'meet'; meetNav.innerHTML = '<span>⌖</span><small>Meet</small>'; document.querySelector('.bottom-nav').insertBefore(meetNav, document.querySelector('.bottom-nav').lastElementChild);
meetNav.addEventListener('click', () => { document.querySelectorAll('.screen').forEach(s => s.classList.toggle('active', s.id === 'meet')); document.querySelectorAll('.nav-item').forEach(n => n.classList.toggle('active', n === meetNav)); });
const inviteModal = document.createElement('div'); inviteModal.className = 'invite-modal'; inviteModal.innerHTML = '<section class="invite-sheet"><button class="invite-close">&times;</button><h2>Invite a player</h2><p>Choose someone to meet at a cafe or play a casual game.</p><button class="player-invite">Nora Aksoy <span>Online now · 0.8 km away</span><b>Invite</b></button><button class="player-invite">Arda Yilmaz <span>Available tonight · 1.6 km away</span><b>Invite</b></button></section>'; document.body.appendChild(inviteModal);
document.querySelector('#invite-button').onclick = () => inviteModal.classList.add('open'); inviteModal.querySelector('.invite-close').onclick = () => inviteModal.classList.remove('open'); inviteModal.querySelectorAll('.player-invite').forEach(b => b.onclick = () => { inviteModal.classList.remove('open'); document.querySelector('#toast').textContent = 'Invitation sent!'; document.querySelector('#toast').classList.add('show'); setTimeout(() => document.querySelector('#toast').classList.remove('show'), 2300); });
function requestLocation(){if(!navigator.geolocation)return;navigator.geolocation.getCurrentPosition(()=>{document.querySelector('#location-status').innerHTML='<span>✓</span><b>Location updated</b><small>distances are now nearby</small>';document.querySelectorAll('.distance').forEach((d,i)=>d.textContent=i?'0.9 km':'0.3 km')})}document.querySelector('#location-button').onclick=requestLocation;document.querySelector('#location-status').onclick=requestLocation;
